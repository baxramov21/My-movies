<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sheyx.mymovies">
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyMovies">
        <activity
            android:name="com.sheyx.mymovies.screens.FavouriteActivity"
            android:label="Любимые фильмы"
            android:exported="false" />
        <activity
            android:name="com.sheyx.mymovies.screens.DetailActivity"
            android:label="Подробно о фильме"
            android:exported="false" />
        <activity
            android:name="com.sheyx.mymovies.screens.MoviesListActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>